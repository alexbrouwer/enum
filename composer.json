{
    "name": "par/enum",
    "description": "Strongly typed enums",
    "type": "library",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "license": "MIT",
    "config": {
        "sort-packages": true,
        "platform": {
            "php": "7.2"
        }
    },
    "autoload": {
        "psr-4": {
            "PAR\\Enum\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PARTest\\Enum\\": "test/"
        }
    },
    "require": {
        "php": "^7.2",
        "par/core": "^0.1"
    },
    "require-dev": {
        "par/coding-standard": "^1.0",
        "par/maker": "^1.0",
        "phpstan/phpstan": "^0.11.8",
        "phpunit/phpunit": "^8.1",
        "roave/security-advisories": "dev-master"
    },
    "scripts": {
        "analyse": "phpstan analyse --no-progress",
        "check": [
            "@cs-check",
            "@test"
        ],
        "cs-check": "phpcs",
        "cs-fix": "phpcbf",
        "test": "phpunit --colors=always",
        "test-coverage": "phpunit --colors=always --coverage-clover clover.xml"
    }
}
